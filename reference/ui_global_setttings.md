# UI - Global Settings

Creates the UI elements with the global settings for the UI components
inside Starlng Shiny application.

## Usage

``` r
ui_global_setttings()
```

## Note

This function is a shiny module function and should be used in the
context of the app created using the `starlng_write_app` function.

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/module_processing.R
\name{get_closest_node_to_module}
\alias{get_closest_node_to_module}
\title{Find Closest Trajectory Node to Module}
\usage{
get_closest_node_to_module(
  trajectory_object,
  cell_umap,
  module_expr,
  expression_threshold = 0,
  expression_percentile = 0,
  scale = TRUE
)
}
\arguments{
\item{trajectory_object}{A trajectory object containing node positions.}

\item{cell_umap}{A cell embedding matrix or data frame with two dimensions.}

\item{module_expr}{A numeric vector with the aggregate expression of the module.
If a logical vector is provided, it will be interpreted as the mask indicating
the module population. Providing a list will return a named vector of closest
nodes for each module.}

\item{expression_threshold}{Expression threshold used to define module-active
cells. Will not be used if \code{module_expr} is already logical. Defaults to 0.}

\item{expression_percentile}{Optional percentile threshold for module-active
cells. Will be applied after \code{expression_threshold} if provided. Defaults to 0.}

\item{scale}{Logical indicating whether expression values should be scaled
between 0 and 1. Will not be used if \code{module_expr} is already logical.
Defaults to TRUE.}
}
\value{
The closest node name, or a named vector of node names for list
input.
}
\description{
Finds the closest trajectory node to a module using the UMAP
distance between the module centroid and the node location.
}
